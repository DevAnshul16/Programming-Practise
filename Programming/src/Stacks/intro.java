package Stacks;

import java.util.Stack;

public class intro {
    public static void main(String[] args) {
        Stack<Integer> st = new Stack<>();
        for(int i = 0 ; i<5; i++){ // for inserting element from 1 to 5
            st.push(i+1);
        }
        /* st.size()- gives size
        st.peek() - gives top element
        st.pop()- return top element and delete it
         */


    }
}
